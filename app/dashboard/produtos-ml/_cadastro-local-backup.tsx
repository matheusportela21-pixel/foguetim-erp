/**
 * BACKUP — Código do cadastro local de produtos
 *
 * Componentes preservados da versão anterior de /dashboard/produtos.
 * Serão reaproveitados no módulo Armazém.
 *
 * Inclui: HealthBar, StockBadge, MktBadges, CopyModal
 * E o bloco `view === 'local'` da ProdutosPage (tabela com filtros, paginação, etc.)
 *
 * Para ver o código completo, veja o git history de:
 * app/dashboard/produtos/page.tsx
 */

// TODO: Quando o módulo Armazém for implementado, mover este código para lá.
export {}
